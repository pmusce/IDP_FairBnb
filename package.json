{
  "name": "IDP_fairbed",
  "private": true,
  "version": "0.0.0",
  "description": "fairbed platform",
  "repository": "https://github.com/pmusce/IDP_fairbed",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.7.7",
    "http-server": "^0.9.0"

  },
  "scripts": {
    "postinstall": "bower install",

    "update-deps": "npm update",
    "postupdate-deps": "bower update",

    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1"
 }
}
